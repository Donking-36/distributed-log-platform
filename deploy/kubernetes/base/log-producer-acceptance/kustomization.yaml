apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - api-service-job.yaml
  - worker-service-job.yaml

components:
  # 与持续 Deployment 共享唯一镜像版本来源，不共享生命周期配置。
  - ../../components/log-producer-image
