apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - client-service.yaml
  - headless-service.yaml
  - statefulset.yaml

components:
  - ../../components/elasticsearch-image
